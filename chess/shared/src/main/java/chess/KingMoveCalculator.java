package chess;

import java.util.ArrayList;
import java.util.Collection;

public class KingMoveCalculator implements MovementRule {
    public Collection<ChessMove> moves(ChessBoard board, ChessPosition myPosition) {
        //calculate king moves based on the position on the board
        return new ArrayList<>(); //Return the datastructure with the king moves.
    }
}
